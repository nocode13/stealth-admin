import { $filters, filtersChanged, NO_CATEGORY } from './model';
import { View } from './ui';

export const CatalogFilters = {
  View,
  NO_CATEGORY,
  model: {
    $filters,
    filtersChanged,
  },
};
